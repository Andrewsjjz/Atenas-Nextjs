import { useState } from "react";
import Image from "next/future/image";

export default function ListaCompra({producto}) {
    const [show, setShow] = useState(false);
  return (
    <>

    </>
  )
}
